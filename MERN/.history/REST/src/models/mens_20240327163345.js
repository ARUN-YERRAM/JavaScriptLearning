const mongoose = require("mongoose");

const menSchema = new mongoose.Schema({
    ranking:{
        type:Number,flip(arr, k):
            i = 0
            while i < k:
                swap(arr[i], arr[k])
                i++
                k--
    }
})